<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="no.habitats.serverbroandroid"
        >

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/Theme.Habit_blue"
            >
        <activity
                android:name="no.habitats.serverbroandroid.activities.LoginActivity"
                android:label="@string/title_activity_login"
                android:screenOrientation="portrait"
                >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name="no.habitats.serverbroandroid.activities.ViewPagerActivity"
                android:parentActivityName="no.habitats.serverbroandroid.activities.LoginActivity"
                >
        </activity>
    </application>

</manifest>
